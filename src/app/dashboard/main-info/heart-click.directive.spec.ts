import { HeartClickDirective } from './heart-click.directive';

describe('HeartClickDirective', () => {
  it('should create an instance', () => {
    const directive = new HeartClickDirective();
    expect(directive).toBeTruthy();
  });
});
